# OmniMind — Preview deployment (per-PR / branch previews)

OMNIMIND_ENV=preview
REDIS_ENABLED=true
REDIS_URL=${REDIS_URL}
JWT_SECRET_KEY=${JWT_SECRET_KEY}
ALLOWED_ORIGINS=${PREVIEW_URL}
OMNIMIND_PUBLIC_API_URL=${PREVIEW_API_URL}
UVICORN_WORKERS=1
