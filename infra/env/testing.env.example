# OmniMind — Testing environment (CI / automated tests)

OMNIMIND_ENV=testing
REDIS_ENABLED=false
JWT_SECRET_KEY=ci-smoke-test-secret
ALLOWED_ORIGINS=http://localhost:3000
MONGODB_URI=
ELASTICSEARCH_ENABLED=false
STREAMING_LAZY_LOAD=true
